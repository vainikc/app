# Vercel serverless dependencies for api/index.py.
#
# Deliberately much smaller than backend/requirements.txt: that file pinned the
# whole Emergent base image (pandas, numpy, boto3, google-genai, litellm,
# emergentintegrations, pytest, linters...), which blows past Vercel's 250 MB
# unzipped bundle limit. Only packages actually imported by backend/*.py are
# listed here.
#
# Every version below was resolved against public PyPI. Several of Emergent's
# pins (dnspython==2.8.0, limits==5.8.0, ...) only exist on their private
# mirror and fail a normal pip install.
fastapi==0.110.1
starlette==0.37.2
pydantic==2.13.4
email-validator==2.3.0
motor==3.3.1
pymongo==4.6.3
dnspython==2.7.0
httpx==0.28.1
python-dotenv==1.2.1
bcrypt==4.1.3
PyJWT==2.13.0
slowapi==0.1.10
python-multipart==0.0.20
